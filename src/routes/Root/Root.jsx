import Map from '../../components/Map/Map';
import StyledRoot from './StyledRoot';

const Root = () => {
  return (
    <StyledRoot>
      <h2>Home</h2>
      <Map />
    </StyledRoot>
  );
};

export default Root;
